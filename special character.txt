// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num;
    printf("Enter character:");
    scanf("%d",&num);
    if(num==36){
        printf("%c",num);
    }
    else if(num==64){
        printf("%c",num);
    }
    else{
        printf("%d",num);
    }
    return 0;
}