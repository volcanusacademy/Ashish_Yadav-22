// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // declared variable
    int num;
    printf("Enter table number:");
    scanf("%d",&num);
    // for loop for desplay given number of table
    for(int i =1;i<11;i++){
        printf("%d x %d = %d\n",num,i,num*i);
    }
    return 0;
}