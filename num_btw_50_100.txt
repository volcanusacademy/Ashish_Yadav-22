// Online C compiler to run C program online
#include <stdio.h>

int chEck(int num){
    if(num>=50 && num<=100){
        printf("yes number in between 50-100\n");
    }
    else{
        printf("Number is not in between 50-100\n");
    }
}

int main() {
    int num;
    printf("Enter number:");
    scanf("%d",&num);
    
    chEck(num);
    return 0;
}