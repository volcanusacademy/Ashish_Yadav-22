// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>

int main() {
    // strlen function fing length of a string
    char name[]={"ashish"};
    int length = strlen(name);
    printf("Length of string is:%d",length);
    
    printf("\n");
    // strcat function concatenate two string joined two string
    char Name[]="Ashish";
    char last_name[]="Yadav";
    strcat(Name,last_name);
    printf("concatenate two string:%s",Name);
    
    printf("\n");
    //strcpy copy string
    char copy[]="ashish";
    char paste[6];
    strcpy(paste,copy);
    printf("copy of string:%s",paste);
    
    printf("\n");
    // strcmp compare two string
    char first_str[]="ashish";
    char second_str[]="ashish";
    // if both string equal return 0 if not return 1
    if(strcmp(first_str,second_str)==0){
        printf("Both are equal");
    }
    else{
        printf("Both are not equal");
    }
    return 0;
}